import React from "react";

const Team = ({ fullName, designation, gender, teamName, id }) => {
  return (
    //  <section className="group-section">
    //           {showInfo &&
    //             members.map((member) => {
    //               return <Team {...member} />;
    //             })}
    //         </section>

    // <article key={id} className="group">
    //   <p>name: {fullName}</p>
    //   <p>{designation}</p>
    //   <p>{teamName}</p>
    //   <p>{gender}</p>
    // </article>
    <>
      <h1>Hello world</h1>
    </>
  );
};

export default Team;
